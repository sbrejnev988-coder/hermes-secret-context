name: secret-context
version: 2.1.0
description: "Memory Wiki secret metadata lookup and metadata-only pre_llm_call injection. Never reads or returns ciphertext/plaintext."
author: "sbrejnev988-coder"
license: "MIT"

hooks:
  - pre_llm_call

provides_tools:
  - secret_context_lookup
  - secret_context_search
